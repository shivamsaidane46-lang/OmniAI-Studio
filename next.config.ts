import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Ready for static deployment (GitHub Pages, etc.)
  // Uncomment these when deploying statically:
  // output: "export",
  // images: { unoptimized: true },
};

export default nextConfig;
